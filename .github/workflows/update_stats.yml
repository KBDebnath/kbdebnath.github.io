name: Update Academic Metrics

on:
  push:
    paths:
      - 'metrics.json'  # Trigger only when you change the numbers
  workflow_dispatch:     # Trigger manually from the "Actions" tab

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run update script
        run: python update_metrics.py

      - name: Commit and push changes
        run: |
          git config --global user.name 'GitHub Action'
          git config --global user.email 'action@github.com'
          git add publications.html
          git diff --quiet && git diff --staged --quiet || (git commit -m "Sync metrics from JSON" && git push)
